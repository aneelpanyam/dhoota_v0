# S3.4 — Session tracing (capture layer)

## Copy-paste prompt

```
Implement S3.4 — Session tracing for Simple Path.

Migration: session_traces (session_id, user_id, space_id, trace_id, action_type, action_params, result, created_at).
Capture layer: record actions in server actions (create_activity, view_feed, etc.).
Store result (success/error, sanitized payload). session_id from client; propagate trace_id.
Indexed for lookup by session_id, trace_id, user_id.

Reference: PLAN.md (Drillable session tracing), AGILE_PLAN.md (S3.4). Depends on S1.3, S3.2.
```

## Context to provide

- `@PLAN.md` — Observability, session_traces
- `src/lib/tracing.ts` — trace_id propagation
- Activity creation server action from S3.2
- Feed/page load flows to instrument

## Deliverables

`supabase/migrations/005_session_traces.sql`, `src/lib/session-tracing.ts`, instrumentation in server actions
