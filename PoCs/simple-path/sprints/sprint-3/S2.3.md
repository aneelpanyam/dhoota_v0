# S2.3 — Channels list (real categories)

## Copy-paste prompt

```
Implement S2.3 — Channels list with real categories for Simple Path.

Channels page loads categories for user's space. Each channel shows title and basic snapshot (e.g. activity count).
Tap channel → navigates to category feed (placeholder ok). Empty state when no categories.

Reference: PLAN.md (Channels, Key Flows), AGILE_PLAN.md (S2.3). Depends on S2.2, S1.7.
```

## Context to provide

- `@PLAN.md` — Channels = category list
- `src/app/(app)/channels/page.tsx` — Current placeholder from S1.7
- `supabase/migrations/003_categories.sql` — categories table
- Categories CRUD from S2.2

## Deliverables

`src/app/(app)/channels/page.tsx` (real data), category list component
