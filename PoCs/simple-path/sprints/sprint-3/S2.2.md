# S2.2 — Categories (workspaces) schema

## Copy-paste prompt

```
Implement S2.2 — Categories (workspaces) schema for Simple Path.

Migration: categories, category_questions, category_answers, category_catalog.
Categories: title, description, notes; space-scoped. Category catalog: system pre-defined workspaces.
RLS; CRUD with validation.

Reference: PLAN.md (Data Model, Category structure), AGILE_PLAN.md (S2.2). Depends on S1.2.
```

## Context to provide

- `@PLAN.md` — Data model, Category = workspace
- `supabase/migrations/` — Existing migrations, RLS patterns

## Deliverables

`supabase/migrations/003_categories.sql`, server actions
