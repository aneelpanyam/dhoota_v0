# S1.5 — Auth flow (access code + OTP)

## Copy-paste prompt

```
Implement S1.5 — Auth flow (access code + OTP) for Simple Path.

- Access code entry page
- OTP request and verification flow
- Session created on success; redirect to channels
- Session timeout config (JWT expiry)
- Invalid code/OTP shows clear error with trace_id
- Log auth attempts (success/failure) to logger

Reference: PLAN.md (Auth, Key Flows), AGILE_PLAN.md (S1.5). Depends on S1.2, S1.3, S1.4.
```

## Context to provide

- `@PLAN.md` — Auth flow, Data model
- `@sprints/sprint-1/S1.2.md` — Core schema (access_codes, users)
- `src/lib/tracing.ts`, `src/lib/logger.ts` — From S1.3
- `src/lib/validation/` — From S1.4
- `supabase/migrations/001_core_schema.sql` — access_codes table structure

## Deliverables

`src/app/(auth)/access-code/page.tsx`, `src/app/(auth)/otp/page.tsx`, auth server actions
