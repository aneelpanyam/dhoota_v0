# S5.1 — S3 file uploads (presigned URLs)

## Copy-paste prompt

```
Implement S5.1 — S3 file uploads for Simple Path.

Presigned URL flow: client requests URL, uploads to S3.
Store file references in activity attachments (JSON).
Validate file types and sizes.
Display images in activity feed and detail.

Reference: PLAN.md (Storage, Activity attachments), AGILE_PLAN.md (S5.1). Depends on S3.1, S3.2.
```

## Context to provide

- `@PLAN.md` — S3, presigned URLs, Activity attachments
- `supabase/migrations/004_activities.sql` — attachments column
- Activity creation form from S3.2
- Activity feed and detail from S3.3, S3.5

## Deliverables

`src/lib/s3/`, presigned URL API, attachment UI in activity form
