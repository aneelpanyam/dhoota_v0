# S1.6 — Non-intrusive status updates (toast)

## Copy-paste prompt

```
Implement S1.6 — Non-intrusive status updates for Simple Path.

Create a toast component (or integrate a toast lib) with:
- Bottom or corner placement
- States: loading ("Saving…"), success (auto-dismiss), error (persist with trace_id copy option)
- No blocking modals
- Accessible and mobile-friendly

Reference: PLAN.md (Status updates in UI/UX), AGILE_PLAN.md (S1.6). Follow acceptance criteria exactly.
```

## Context to provide

- `@PLAN.md` — Status updates UX guidelines
- `src/components/` — Project structure
- `src/lib/tracing.ts` — For trace_id in error toasts (if S1.3 done)

## Deliverables

`src/components/ui/StatusToast.tsx` (or toast lib integration)
