# S3.1 — Activities schema and questions

## Copy-paste prompt

```
Implement S3.1 — Activities schema and questions for Simple Path.

Migration: questions (per category), activities, activity_answers.
Question types: file, table, date, email, phone, rich_text.
Activities: raw_description, attachments (JSON refs), category_id, created_by.
activity_answers: activity_id, question_id, value (JSONB), question_key.
RLS; indexes for common queries.

Reference: PLAN.md (Activity Model, Data Model), AGILE_PLAN.md (S3.1). Depends on S2.2.
```

## Context to provide

- `@PLAN.md` — Activity Model, questions, activity_answers
- `supabase/migrations/003_categories.sql` — categories, FK to category_id

## Deliverables

`supabase/migrations/004_activities.sql`
