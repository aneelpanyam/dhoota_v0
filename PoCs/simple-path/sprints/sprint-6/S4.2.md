# S4.2 — LLM cost logging

## Copy-paste prompt

```
Implement S4.2 — LLM cost logging for Simple Path.

Migration: llm_cost_logs (space_id, user_id, provider, model, operation, input_tokens, output_tokens, latency_ms, trace_id, created_at).
Wrapper logs every LLM call before/after.
Also log to CloudWatch for alerting.
Admin can view usage/cost dashboard (basic table or chart).

Reference: PLAN.md (LLM cost, Observability), AGILE_PLAN.md (S4.2). Depends on S4.1, S1.3.
```

## Context to provide

- `@PLAN.md` — llm_cost_logs, Observability
- `src/lib/llm/` — LLM wrapper from S4.1
- `src/lib/logger.ts` — From S1.3

## Deliverables

`supabase/migrations/007_llm_cost_logs.sql`, logging wrapper, admin dashboard stub
